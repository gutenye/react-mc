#!/usr/bin/env bash

export PATH="node_modules/.bin:$PATH"

build() {
  npm run build
}

docs() {
  styleguidist server
}

humantest() {
  webpack-dev-server --config humantest/webpack.config.js
}

case "$1" in
  b | build ) build ;;
  humantest ) humantest ;;
  docs ) docs ;;
esac
